{
  "name": "antoineview",
  "version": "1.0.0",
  "description": "Personal portfolio. No framework, no client-side JS. Static HTML.",
  "private": true,
  "scripts": {
    "dev": "browser-sync start --server --files \"index.html, css/*.css\" --port 3000 --no-open --no-notify"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4"
  }
}
